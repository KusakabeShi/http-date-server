# http-date-server
A http date server

The source code of website https://wget.date

## Examples
```
curl wget.date
curl wget.date/--help
curl wget.date -d "+%Y/%m/%d"
curl wget.date -d "+\"DATE: %D%nTIME: %T\""
```
